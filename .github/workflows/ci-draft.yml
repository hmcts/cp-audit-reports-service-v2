name: Build and Publish (Non-Release)

on:
  pull_request:
    branches:
      - master
      - main
  push:
    branches:
      - master
      - main

jobs:
  ci-draft:
    uses: ./.github/workflows/ci-build-publish.yml
    secrets: inherit
    with:
      is_publish: true
      trigger_docker: true
      trigger_deploy: true
